
<!DOCTYPE html>
<html>
<head>
	<title>The Gossip Project - Gossips Edit</title>
</head>
<body>
	<h1>Gossips Edit</h1>
    <h2>Modifie ton Gossip</h2>

    <%= form_tag(url_for(action: 'update') , method: "PATCH") do %>
        <div class="form-group col-md-6 mb-3">
            <%= label_tag 'Titre du Gossip' %>
            <%= text_field_tag 'title', @gossip.title, :class => 'form-control' %>
        </div>

        <div class="form-group col-md-6 mb-3">
            <%= label_tag 'Contenu du Gossip' %>
            <%= text_area_tag 'content', @gossip.content, :class => 'form-control', rows: 3 %>
        </div>

        <div class="form-group col-md-6 mb-3">
            <%= submit_tag "Modifier", :class => 'btn btn-primary' %>
        </div>
    <% end %>
</body>
</html>
